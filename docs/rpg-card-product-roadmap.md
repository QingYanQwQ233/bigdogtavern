# Tavern RPG 卡模式产品路线图

> 需求来源：`RPG 卡模式项目大纲`（2026-08-13）
>
> 文档职责：把产品需求拆成“项目大纲 → 模块细纲 → 可执行小任务”，并按当前仓库真实完成度标记。
>
> 架构底座与历史实施记录仍见 [world-card-architecture.md](world-card-architecture.md) 和 [world-card-implementation-plan.md](world-card-implementation-plan.md)。本文是后续 RPG 产品功能的主路线图。

> 当前执行：R5.3 通用状态值校验、R5.4 安全派生值、R5.5 经济规则、R5.6 统一冲突生命周期、R5.7 最小战斗、R5.8 最小非战斗冲突、R5.9 成长候选、R5.10 成长应用、R5.11 动态 RPG 面板、R5.12 题材卡和回归、R6.1 正式事件账本、R6.2 短期上下文窗口、R6.3 长期事件记忆提取、R6.4 世界卡稳定设定 / 存档变更事实分层、R6.5 按场景、NPC、目标和预算组装上下文、R6.6 派生记忆重建和调试视图与 R6.7 失败与死亡模式 Schema 均已完成，下一步进入 R6.8 开放式结局。相关 GitHub 检索未发现可直接兼容本项目的零依赖实现，因此不引入新依赖。

## 0. 结论

当前项目已经有一套较可靠的**世界卡 / 世界存档底座**，但还不是需求中定义的完整沙盒 RPG：

- 已有：世界卡与存档隔离、地图与图片归档、不可变世界版本、回合 CAS / 幂等提交、NPC 知识边界、临时实体、导入导出、旧 RPG 会话迁移。
- 部分已有：世界编辑器、自由文本行动、Markdown 叙事、NPC 动态状态、背包、任务、状态栏、失败重试。
- 主要缺失：按卡定义的玩家创建、动态能力 / 资源 / 特质、可信判定流程、NPC 主动生活、世界时间与事件、通用冲突、成长、分层记忆、失败 / 死亡 / 结局、动态信息 UI。
- 因此，后续不再沿“再补一个底层接口”的顺序推进，而按玩家可感知闭环推进：**建角开局 → 一次可信行动 → NPC 持续反应 → 世界随时间演化 → 系统与成长 → 长期故事与结局 → 平台验收**。

### 状态标记

| 标记 | 含义 |
|---|---|
| ✅ 已验证 | 已有实现，并有当前代码或自动检查证据 |
| 🟡 部分完成 | 只完成数据层、固定版本或部分 UI，尚未形成需求中的闭环 |
| 🧱 仅底座 | 能支撑后续功能，但玩家还不能直接使用该功能 |
| ⬜ 未开始 | 当前代码没有对应的正式能力 |
| ⚠️ 待实机验证 | 静态 / Node 检查通过，但真实浏览器或 Android 尚未验证 |

## 1. 项目大纲

### 1.1 产品定义

RPG 卡不是“只有开场白的角色卡”，而是一个**可运行的世界种子**：

```text
RPG 卡（WorldCard）
  定义世界、规则、玩家创建、NPC、游戏系统、AI 行为与 UI 投影
    ↓ 创建
独立存档（WorldSave）
  保存玩家、时间、世界事实、NPC 状态、地图、记忆与已提交回合
    ↓ 每次行动
观察 → 玩家自由输入 → 行动解析 → 必要时判定 → 世界反馈 → 原子提交 → 新局面
```

AI 同时承担世界模拟器、GM / DM、NPC、叙事者和裁定助手，但不能替玩家决定核心意图。程序负责身份归属、规则边界、随机数、校验、提交与恢复。

### 1.2 十个产品模块

| 编号 | 模块 | 对应原需求 | 当前状态 |
|---|---|---|---|
| M0 | 产品契约与数据所有权 | 项目定位、最终原则、RPG 卡定义 | 🟡 原则清楚，产品契约未版本化 |
| M1 | RPG 卡定义与作者工作台 | 世界准备、RPG 卡应包含什么 | 🟡 仅元数据、地点、NPC、地图可编辑 |
| M2 | 玩家创建、初始关系与开场 | 开局准备、开场生成 | 🟡 已加入声明式 `playerCreation` 与动态存档输入，AI 开场与更丰富规则待后续阶段 |
| M3 | 回合内核、行动解析与判定 | 核心循环、判定系统、AI 行为原则 | 🟡 有自由输入、行动意图原文、卡片选项契约和原子提交，正式解析 / 判定待后续阶段 |
| M4 | NPC、关系与认知 | NPC 系统、初始人物关系 | 🟡 已支持欲望、恐惧、目标、活动等声明式资料进入当前作用域；主动生活与记忆闭环待后续 |
| M5 | 世界时间、事件与目标 | 世界演化、任务与故事 | 🟡 已有卡片时间声明、声明式事件和服务端回合结算；时限目标与新闻投影待后续 |
| M6 | 冲突、资源与成长 | 战斗 / 冲突、角色成长 | 🧱 只有固定 HP / MP / 等级 / 金币 |
| M7 | 状态、记忆与上下文 | 状态与记忆、世界连续性 | 🟡 有历史与状态，无短期 / 长期 / 稳定事实分层 |
| M8 | 叙事界面与信息投影 | 信息展示、选择与自由行动 | 🟡 固定基础状态外已开始按卡片 schema 投影动态属性 / 资源；完整可见性与分组声明待后续 |
| M9 | 存档、失败、结局与平台 | 存档、死亡与失败、故事结束 | 🟡 存档底座强，玩法语义和 Android 尚不完整 |

### 1.3 不可违反的产品不变量

1. `WorldCard` 只定义世界种子；一切可变化事实必须写入明确的 `saveId`。
2. 一个存档是一条独立世界线。玩家、NPC、关系、地图、图片、时间、选项、状态、记忆和事件不能跨存档串联。
3. 酒馆仍是 `CharacterCard → ChatSession`；RPG 是 `WorldCard → WorldSave`，两条历史与 Prompt 路径不得互读。
4. 玩家拥有角色。AI 可描述已发生的直接后果，不能替玩家选择核心意图、台词、情感立场或不可逆行动。
5. AI 输出只是候选。只有通过规则校验并成功写入存档的变化才是正式事实。
6. 随机数由程序产生并记录，AI 不能自行宣称掷骰结果。
7. RPG 卡扩展必须是声明式 JSON；导入的 HTML、JavaScript、EJS、MVU 或脚本默认不执行。
8. 默认数据继续来自 `_defaults.json`，不能把世界内容、属性名、资源名、规则或 UI 栏目写死在前端。
9. 选项只是建议，自由文本输入始终可用，且不能被选项范围限制。
10. 失败必须生成新局面；是否永久死亡、重伤、俘虏或回退由卡规则定义。

## 2. 模块细纲

### M0. 产品契约与数据所有权

**目标**：明确卡、存档、回合、事实、事件、记忆和 UI 的唯一 owner，避免 AI 文本或全局变量成为事实来源。

**现状**：`worldId + worldVersion`、`saveId + revision`、`charId + kind` 已建立；回合具备 `commandId`、receipt、CAS 和原子写入。世界模式仍把固定 `rpgState` 映射成 HP / MP 等字段，缺少版本化玩法契约。

**细分能力**：

- `WorldCard.schemaVersion`：卡结构版本与迁移规则。
- `turnContractVersion`：行动解析、判定、状态候选、事件和建议选项的输出契约。
- `EntityRef`：世界实体与存档生成实体的稳定 ID 规则。
- `CommittedFact`：只有成功提交的状态变化、事件和消息可进入记忆。
- `Projection`：UI 只读取存档投影，不直接从叙事文本猜状态。

**完成定义**：任一界面字段都能追溯到卡定义或当前存档；任一 AI 失败、解析失败或保存失败都不会制造半提交事实。

### M1. RPG 卡定义与作者工作台

**目标**：作者可用 JSON 和可视化工作台定义一个完整世界种子，而不是只能编辑简介、地点和 NPC。

**现状**：世界草稿支持元数据、世界书 ID、地点、NPC 和地图参数；发布形成不可变版本；世界包可导入导出。尚无玩家创建规则、动态属性、资源、特质、派系、物品、事件、冲突、成长、死亡、结局和 UI 定义。

**卡内模块**：

- `setting`：历史、地理、国家、城市、种族、宗教、文化、科技 / 魔法、社会、经济、法律和当前局势。
- `rules.hard / rules.soft`：硬规则进入校验与裁定，软规则进入 AI 行为约束。
- `playerCreation`：身份字段、属性、点数、范围、技能、天赋、资源和初始关系。
- `entities`：NPC、派系、地点、物品、事件模板和目标模板。
- `systems`：判定、时间、冲突、成长、经济、死亡与结局规则。
- `storyControl`：开场约束、禁用的强制剧情、失败推进方式和结局条件。
- `aiProfile`：身份、玩家主权、知识范围、公平性和输出契约。
- `ui`：声明式分组、字段可见性、顺序和格式，不携带可执行代码。

**完成定义**：换一张卡即可得到不同字段、资源、判定和 UI；前端不需要为“魔力 / 理智 / 赛博义体 / 声望”等题材字段改代码。

### M2. 玩家创建、初始关系与开场

**目标**：玩家按卡作者定义的规则创建角色，经校验后生成只属于该存档的角色快照，再由世界与角色共同生成开场局面。

**现状**：服务端已支持声明式 `playerCreation` 校验，并把用户输入规范化为存档内 `player.snapshot` 与 `state.player`；前端动态建角向导、初始关系输入和 AI 开场幂等提交已接入。互斥条件和更丰富的技能规则留到后续阶段。

**细分流程**：

1. 读取卡的 `playerCreation`，渲染动态身份、属性、技能、特质和资源字段。
2. 实时显示点数预算、互斥条件、范围和必填错误。
3. 玩家确认后由服务端校验，生成不可被其他存档改写的 `player.snapshot`。
4. 初始化 NPC 双向关系、初始地点、时间、资源、物品和已知事实。
5. AI 根据世界当前局势、玩家身份和开场规则生成开场候选。
6. 开场通过协议校验并提交为 revision 1；失败可重试，不污染存档。

**完成定义**：同一世界创建两份不同角色存档，建角字段、开场、关系和状态各自恢复且互不影响。

### M3. 回合内核、行动解析与判定

**目标**：真正实现“观察 → 自由行动 → 解析 → 必要时判定 → 反馈 → 状态提交”的可信循环。

**现状**：自由文本、建议选项、Markdown、流式草稿、严格存档提交和失败重试已有；`DICE_RE` 仍从文本识别骰子并立即随机，判定依据和结果等级没有正式结构，核心状态字段写死。

**目标回合阶段**：

```text
PlayerCommand
  → ActionIntent（行为 / 目标 / 方法 / 风险）
  → CheckProposal（无判定，或属性 / 技能 / 难度 / 因素）
  → ProgramRoll（程序产生并记录随机数）
  → ResolutionCandidate（六级结果 + 叙事 + 状态差异 + 事件）
  → Validate
  → Commit(saveId, expectedRevision, commandId)
  → UI / Memory projections
```

**判定要求**：

- 没有不确定性或结果差异无意义时不掷骰。
- 判定因素可引用属性、技能、特质、装备、环境、NPC、难度、状态与随机数。
- 标准结果支持：大成功、成功、有代价的成功、部分成功、失败、严重失败。
- 判定依据对玩家给出简短可解释摘要，不暴露隐藏推理。
- 解析 / 模型 / 网络 / 校验 / CAS 失败时 revision 不变，并提供明确恢复动作。

**完成定义**：完成一次无需判定行动和一次需要判定行动；重试不重复奖励；篡改未知实体或越界数值被拒绝。

### M4. NPC、关系与认知

**目标**：NPC 是有持续目标和有限认知的角色，不是玩家的功能按钮，也不共享全知视角。

**现状**：世界 NPC、存档 NPC 状态、队伍、当前地点 / 任务作用域、秘密解锁、临时 NPC 和晋升已有底座；关系只是任意对象，缺少正式维度、记忆、计划、主动行为和 UI。

**NPC 定义**：身份、外形、性格、说话方式、欲望、恐惧、利益、秘密、公开目标、长期目标、关系倾向和活动范围。

**NPC 存档状态**：当前位置、状态、双向关系、已知事实、个人记忆、当前目标、计划、承诺、敌意、资源与最近活动。

**运行规则**：

- 只注入当前场景、队伍、相关目标或近期事件涉及的 NPC。
- NPC 只能使用公开资料和自己在该存档中已知的事实。
- NPC 主动行为在已提交的时间推进点结算，不需要后台常驻进程。
- 关系是双向且可不对称的；数值变化必须伴随可说明的事件。

**完成定义**：NPC 离场后仍保持自己的目标和记忆；同一 NPC 在两个存档中的位置、关系和知识完全独立。

### M5. 世界时间、事件与目标

**目标**：世界在玩家行动之间保持连续，并在时间推进时发生与玩家无关的变化。

**现状**：只有消息时间戳和更新时间，没有游戏内时钟、行动耗时、调度事件、世界新闻或历史事实账本；任务仍是固定列表。

**细分能力**：

- `worldClock`：历法、当前时间、时区 / 地区和显示格式。
- `timeCost`：移动、休息、制作、冲突和长行动消耗时间。
- `scheduledEvents`：明确时间或条件触发的事件。
- `worldEvents`：已发生事件、参与者、地点、公开程度、后果和来源 revision。
- `goals / leads`：条件、行动者、动机、时限和当前状态，不锁定 A→B→C 流程。
- `offscreenChanges`：只在提交边界按规则结算相关 NPC / 派系，不做真实时间后台模拟。

**完成定义**：玩家忽略一个带时限目标后，时间推进触发合理后果；重开存档后时间、事件与后果一致。

### M6. 冲突、资源与成长

**目标**：用同一套可配置框架处理战斗、谈判、潜行、追逐、审讯、黑客、政治和商业等冲突。

**现状**：固定 HP、MP、EXP、等级、金币、buff、背包和任务；卡无法定义其他资源或冲突流程。

**声明式系统**：

- `attributes / skills / traits`：显示名、类型、范围、默认值、派生关系和用途说明。
- `resources`：当前值、上限、恢复、耗尽后果和可见性。
- `checks`：骰制、难度、修正、优势 / 劣势与结果梯度。
- `conflicts`：参与者、目标、轮次 / 阶段、可用行动、退出条件与后果；不强制所有冲突回合制。
- `inventory / economy`：物品定义、数量、装备位、货币、负重或题材自定义约束。
- `growth`：训练、学习、探索、关系、事件和冲突带来的能力、身份、声望或资源变化。

**完成定义**：至少用同一内核跑通一个战斗冲突和一个非战斗冲突；成长结果写入人物经历和世界关系，而非只显示“等级 +1”。

### M7. 状态、记忆与上下文

**目标**：长线游戏能记住重要事实，又不把全部历史、全部 NPC 和全部世界书塞进每次请求。

**现状**：已提交时间线、玩家状态、NPC 状态、世界书触发和全局手工记忆存在；没有存档级短期记忆、长期事件记忆、稳定事实、摘要来源和重建机制。

**四层结构**：

1. `turns`：原始已提交历史，永远是来源证据。
2. `recentContext`：最近回合和当前场景，按窗口构建，不作为独立事实 owner。
3. `eventMemory`：从已提交事件提取的长期记忆，带实体、地点、时间和来源 revision。
4. `stableFacts`：世界长期成立或已被正式改变的事实；世界卡原始设定与存档变更分开保存。

**完成定义**：超过上下文窗口后，NPC 仍能正确引用关键承诺；删除派生摘要并重建不会改变正式事实。

### M8. 叙事界面与信息投影

**目标**：正文保持连续叙事，规则信息以可验证的独立 UI 展示，并且界面由卡的声明式定义驱动。

**现状**：RPG 正文支持 Markdown，无酒馆气泡正则；有固定角色 / 背包 / 任务 / 地图 / HP / MP / EXP / 金币界面和独立调试窗口。

**目标界面**：

- 中央叙事：已提交、生成中、失败草稿和待重试状态明确区分。
- 行动区：自由输入始终是主入口，建议行动是可选快捷方式。
- 判定卡：显示使用的能力、主要修正、随机数、难度和结果等级。
- 动态状态：按卡定义分组展示属性、资源、特质、装备、效果与关系。
- 世界投影：时间、地点、地图、事件、目标、NPC 和派系变化。
- 调试窗口：显示实际 Prompt、原始输出、解析结果、候选差异和提交结果，并默认脱敏。

**完成定义**：更换到使用不同属性 / 资源的测试卡后，UI 无需改代码即可正确显示；键盘、窄屏、焦点和 reduced-motion 可用。

### M9. 存档、失败、结局与平台

**目标**：让完整世界线可以安全创建、继续、失败、结束、导出和跨平台恢复。

**现状**：Web 存档支持创建、打开、版本升级、导入导出、迁移、原子写入和恢复；删除 / 复制 / 分支 / 总结未做；Android 内嵌服务器仍只有旧数据 API。

**细分能力**：

- 存档操作：重命名、复制、导出、显式删除和损坏恢复。
- 失败模式：继续、重伤、俘虏、资源损失、永久死亡或卡定义流程。
- 结局：玩家主动结束、规则条件触发或开放式继续。
- 世界线总结：只读取已提交事件与事实，生成可追溯的经历、关系和世界变化摘要。
- 平台：Web / PWA 先稳定，Android 在 API 契约冻结后补齐。

**完成定义**：完整跑通“建角 → 开场 → 多回合 → 失败分支 → 结束 → 总结 → 重开查看”；Web 与 Android 使用同一数据语义。

## 3. 小任务拆分

任务按纵向可玩切片排序。每个阶段都必须同时包含最小的数据、规则、UI 和恢复验证，不先铺一整套永远用不到的抽象。

### B0. 已有底座盘点

| ID | 小任务 | 状态 | 验收证据 |
|---|---|---|---|
| B0.1 | 固定 Tavern 与 RPG 的 owner 边界 | ✅ | `charId + kind` 与 `WorldCard → WorldSave` 分流检查通过 |
| B0.2 | 地图数据、网格和美化图归属存档 | ✅ | `check_session_binding`、`check_world_storage` 通过 |
| B0.3 | 建立世界存档 revision、原子写入和 CAS | ✅ | 过期 revision 返回 409 |
| B0.4 | 建立 command receipt 和幂等重试 | ✅ | 相同 command 不重复写入或奖励 |
| B0.5 | 建立世界 NPC、临时实体和知识边界 | ✅ | 跨世界 / 存档注入检查通过 |
| B0.6 | 建立不可变世界版本、草稿、升级和世界包 | ✅ | 导入导出、哈希、升级检查通过 |
| B0.7 | 建立旧 RPG 会话只读迁移 | ✅ | 迁移幂等且原数据不删除 |
| B0.8 | 建立模型超时和损坏数据恢复错误 | ✅ | `check_recovery` 通过 |
| B0.9 | 真实浏览器与 Android 闭环 | ⚠️ | 浏览器本轮无授权；Android API 尚未对齐 |

### R0. 冻结 RPG 卡 v2 产品契约

**阶段结果**：得到可实现、可迁移、不可执行脚本的卡契约；不在本阶段提前实现全部玩法。

| ID | 小任务 | 依赖 | 验收 |
|---|---|---|---|
| R0.1 | 把原需求逐项映射到 M0–M9 | 无 | 每条原需求都有唯一主模块，不重复归属 |
| R0.2 | 确认第一版只控制一个玩家角色，队伍 NPC 自主行动 | R0.1 | 写入产品决定；多主角控制不进入首版 |
| R0.3 | 确认建议行动策略：默认 4 条、卡可设 0–4 条、自由输入常驻 | R0.1 | 不再由服务端无条件要求恰好 4 条 |
| R0.4 | 定义 `WorldCard schemaVersion: 2` 最小增量和 v1→v2 迁移 | R0.1 | 旧世界卡能无损读取，新增字段有默认值 |
| R0.5 | 定义版本化 `turnContract` 和错误状态 | R0.4 | 流式正文、解析结果、候选、提交结果互不混淆 |
| R0.6 | 建立字段 owner / writer / reader / persistence 清单 | R0.4 | 每个可变字段明确属于一个 `saveId` |
| R0.7 | 为 R1 写行为用例和隔离矩阵 | R0.4 | 用例覆盖两世界、两存档、两种玩家配置 |
| R0.8 | 开工前检索同类建角 / 规则 Schema 项目并记录许可证 | R0.4 | 形成短研究记录；优先复用现有代码与标准能力 |
| R0.9 | 定义玩家主权、公平裁定、知识边界和非恶意针对的 AI 行为契约 | R0.5 | Prompt 用例能识别替玩家决定、偏袒、恶意针对和越权知识 |

### R1. 可创建角色并进入世界

**阶段结果**：玩家能从世界库创建一名符合卡规则的角色，生成并提交开场，刷新后继续。

**R1 研究记录**：检索了 SillyTavern 的 World Info 声明式条目与多个 character-card generator 项目；它们可借鉴字段 / 作用域概念，但没有能直接接入本项目零依赖 Node 服务和 WorldSave owner 链的实现。本阶段因此只复用标准 JSON、原生表单与现有 API，不新增依赖。

| ID | 小任务 | 依赖 | 验收 |
|---|---|---|---|
| R1.1 | 为 `playerCreation` 定义身份字段、属性、资源、特质和初始关系的声明式 JSON | R0 | 测试卡能定义与 HP / MP 无关的字段 |
| R1.2 | 在服务端规范化并校验建角 Schema | R1.1 | 重复 ID、越界默认值、错误预算和未知引用被拒绝 |
| R1.3 | 世界草稿编辑器增加最小建角规则编辑 | R1.2 | 作者能添加、删除、排序字段并保存草稿 |
| R1.4 | 新建存档前显示由 JSON 驱动的建角向导 | R1.1 | 无硬编码栏目；必填、范围、点数即时提示 |
| R1.5 | 支持卡提供预设主角或允许自建，并声明默认入口 | R1.4 | 两类测试卡均能创建存档 |
| R1.6 | `POST /api/world-saves` 接收并二次校验玩家输入 | R1.2 | 客户端篡改预算或初始资源无法落盘 |
| R1.7 | 初始化玩家快照、资源、地点、时间和双向初始关系 | R1.6 | 全部只写入新 `saveId` |
| R1.8 | 构造开场生成上下文和开场候选协议 | R1.7 | 包含时间、地点、在场 NPC、事件和玩家在场原因 |
| R1.9 | 将开场作为首个幂等命令提交 | R1.8 | 模型或保存失败不产生半个开场；可安全重试 |
| R1.10 | 增加两存档建角 / 开场 / 重开回归 | R1.9 | 不同玩家、关系、开场和状态互不串联 |

### R2. 一次可信的自由行动与判定

**阶段结果**：玩家可自由输入行动；系统只在必要时判定，并以可解释、可恢复的方式提交结果。

| ID | 小任务 | 依赖 | 验收 |
|---|---|---|---|
| R2.1 | 开工前检索叙事 RPG 判定与结构化输出实现 | R1 | 记录可借鉴的契约和许可证，不引入不必要依赖 |
| R2.2 | 定义 `ActionIntent`：行为、目标、方法、风险和玩家原文 | R0.5 | AI 不补写玩家未表达的核心意图 |
| R2.3 | 定义 `CheckProposal`：无需判定或引用卡内能力 / 难度 / 因素 | R2.2 | 未登记属性、技能或目标不能进入正式判定 |
| R2.4 | 把随机数从叙事正则迁到程序判定器 | R2.3 | 骰制、随机值和 commandId 被记录；重试不重掷 |
| R2.5 | 支持六级判定结果与简短可解释摘要 | R2.4 | 每级都能形成不同但合理的新局面 |
| R2.6 | 定义通用候选差异：状态、实体、关系、事件、时间和建议行动 | R2.5 | 核心变化不靠读取正文正则产生 |
| R2.7 | 为版本化输出契约实现严格解码和边界校验 | R2.6 | 多代码块、缺字段、未知 ID、超大数组均失败关闭 |
| R2.8 | 复用现有 CAS / receipt 原子提交完整回合 | R2.7 | 叙事、判定、变化和选项同一 revision 落盘 |
| R2.9 | UI 区分解析、掷骰、生成、提交和失败恢复状态 | R2.8 | 用户能看到当前发生在哪一步及可执行恢复动作 |
| R2.10 | 把固定“恰好 4 选项”改为卡配置上限 | R0.3 | 0–4 均可，重复 / 空选项被过滤，自由输入不隐藏 |
| R2.11 | 增加无判定、判定、模型失败、CAS 冲突和幂等回归 | R2.8 | 失败不改正式状态，重试不重复结果 |

### R3. NPC、关系和认知形成闭环

**阶段结果**：一名 NPC 能主动行动、记住玩家行为、改变双向关系，并在两个存档中保持不同人生。

| ID | 小任务 | 依赖 | 验收 |
|---|---|---|---|
| R3.1 | 扩展 NPC 静态 Schema：欲望、恐惧、利益、目标、活动范围和秘密 | R0 | 世界草稿可编辑且发布后不可变 |
| R3.2 | 扩展 `npcStates`：目标、计划、个人记忆、承诺和最近活动 | R2 | 字段只属于当前 `saveId + npcId` |
| R3.3 | 定义双向非对称关系维度及范围 | R3.2 | A→B 与 B→A 可不同，维度由卡定义 |
| R3.4 | 初始关系从建角规则写入玩家与 NPC 两侧 | R1.7 | 新存档初始化正确，其他存档不变化 |
| R3.5 | 关系与知识变化进入通用候选差异 | R2.6 | 只有叙事已发生且引用有效时可提交 |
| R3.6 | 作用域解析器加入当前场景、队伍、目标和近期事件 | R3.2 | 不向 Prompt 注入无关 NPC |
| R3.7 | NPC 主动行为只在提交后的时间推进点计算 | R3.2 | 无后台进程；刷新和重试结果稳定 |
| R3.8 | 为临时 NPC 补最小目标 / 知识 / 关系默认值 | R3.2 | 临时 NPC 不污染世界卡，晋升后保留合法静态资料 |
| R3.9 | 增加 NPC 详情、关系变化和已知事实投影 | R3.5 | 隐藏秘密不会泄露到玩家 UI 或调试导出 |
| R3.10 | 增加跨世界、跨存档和离场后再会回归 | R3.9 | 位置、关系、记忆、秘密均隔离且连续 |

### R4. 世界时间、事件和开放目标

**阶段结果**：时间随行动推进，NPC / 派系 / 地区事件可以独立发生，忽略目标也会产生后果。

| ID | 小任务 | 依赖 | 验收 |
|---|---|---|---|
| R4.1 | 定义卡内历法、时间单位、显示格式和初始时间 | R0 | 不同题材可使用小时、天、月份或自定义历法 |
| R4.2 | 把 `worldClock` 与最近推进来源加入 WorldSave | R4.1 | 所有时间变化带 commandId / revision |
| R4.3 | 为行动、移动、休息和长操作定义 `timeCost` | R2 | 时间消耗由卡规则与裁定共同决定 |
| R4.4 | 定义计划事件和条件事件 Schema | R4.2 | 事件可引用稳定 NPC / 派系 / 地点 ID；已支持 `at` / `afterTurns` / `locationId` 的声明式 AND 条件 |
| R4.5 | 在每次成功提交后结算到期事件 | R4.4 | 重试不重复触发，未提交回合不推进时间；已写入 `state.worldEvents` 与 receipt |
| R4.6 | 定义公开消息、局部变化和隐藏世界事实的可见范围 | R4.4 | 玩家只看到合理可知的后果 |
| R4.7 | 用 `goals / leads` 替代强制 A→B→C 任务 | R4.4 | 已加入存档级目标 / 线索 upsert 与动态投影；条件、行动者、动机、时限和结果的完整结算待后续 |
| R4.8 | 兼容迁移旧 `quests` 到简单目标投影 | R4.7 | 旧存档载入时生成带 `legacy-*` ID 的目标投影，不丢标题、描述和状态；原任务仍保留 |
| R4.9 | 增加时间、事件、目标和世界新闻 UI | R4.6 | 正文与结构化信息分离 |
| R4.10 | 增加忽略时限、长休、跨地区和重开回归 | R4.9 | 世界后果可重复验证且只影响目标存档 |
| R4.11 | 定义派系静态资料与存档级目标、资源、关系和影响力 | R4.4 | 派系定义属于卡，变化只属于当前存档 |
| R4.12 | 在时间推进点结算相关派系行动并投影到事件 / 新闻 | R4.11 | 无关派系不注入当前回合，重复提交不重复行动 |

### R5. 可配置资源、冲突和成长

**阶段结果**：卡作者能用同一声明式机制定义不同题材的数值，并跑通战斗与非战斗冲突。

**R5.1 研究记录（2026-08-13）**：

- **OpenRPG/OpenRpg（MIT）**：采用“静态模板与运行态数据分离”、可组合的效果 / 物品 / 冲突模型作为概念参考；其 .NET 运行时与对象模型不能直接接入本项目的零依赖 Node 服务，也不复制代码。
- **CacheControl/json-rules-engine（ISC）**：采用 `all / any / not` 条件树、事实引用和事件结果的 JSON 形状作为规则 Schema 参考；项目依赖 npm、异步引擎生命周期与本项目的 `WorldSave` 原子提交边界不同，因此不引入。
- **SillyTavern-LALib（仓库按其许可文件执行）**：采用比较运算符白名单和表达式“安全子集”思路；不执行 STScript、模板或任意 JavaScript，避免把可执行内容写入世界卡。
- **最终边界**：R5 只复用标准 JSON、现有 `validate*` 校验器、`WorldCard → WorldSave` owner 链和已有 CAS / receipt；新增规则必须声明式、可拒绝、可追溯，AI 结果仍只是候选，不直接写正式状态。

| ID | 小任务 | 依赖 | 验收 |
|---|---|---|---|
| R5.1 | 开工前检索通用冲突 / 规则 Schema 实现 | R4 | 记录复用与拒绝原因，保持零依赖优先 |
| R5.2 | 定义属性、技能、特质和资源字段 Schema | R1.1 | 已支持卡片声明属性 / 技能 / 资源的类型、范围、默认值、特质与关系；可见性 / 用途待后续 |
| R5.3 | 实现通用状态值校验，替换固定 HP / MP 白名单 | R5.2 | 已校验存档动态属性 / 资源 ID 与范围，并支持 AI 相对变化；固定 HP / MP 兼容投影仍保留 |
| R5.4 | 实现通用派生值与只读显示表达式的安全子集 | R5.2 | 不执行任意 JS；循环依赖和非法引用报错 |
| R5.5 | 定义物品、装备位、货币和可选负重规则 | R5.2 | 已支持世界卡声明物品目录、装备位、货币范围、背包格数 / 重量，并在创建 / 保存 / 回合提交时服务端复核 |
| R5.6 | 定义统一 `ConflictState` 与开始 / 推进 / 退出生命周期 | R2 | 已支持世界卡冲突模板、存档级冲突实例和 start / advance / end CAS 校验；战斗、谈判等仍复用同一内核 |
| R5.7 | 实现一套最小战斗配置 | R5.6 | 已支持声明式攻击 check、服务端 d20 + 属性修正对抗 target/防御、命中后伤害骰扣 HP，并将结果写入 receipt；撤退和失败沿用 R5.6 outcome 生命周期 |
| R5.8 | 实现一套最小非战斗冲突配置 | R5.6 | 已支持 social / stealth 的技能判定与 `conflictChecks` receipt；不读取或扣除 HP，仍沿用同一冲突生命周期 |
| R5.9 | 定义成长来源和候选变化 | R5.2 | 已支持世界卡声明成长来源与候选目标；存档只记录待确认候选，不自动应用数值变化 |
| R5.10 | 把成长写入能力变化、身份 / 声望和人物经历 | R5.9 | 已支持玩家确认 / 拒绝；接受后服务端应用声明变化，并写入可追溯人物经历 |
| R5.11 | 动态渲染属性、资源、特质、装备和冲突面板 | R5.3 | 已按世界卡声明动态投影角色状态、装备目录与冲突实例，更换测试卡无需改前端代码 |
| R5.12 | 增加两种题材卡、两类冲突和成长回归 | R5.11 | 已加入现代悬疑 / 科幻轨道站示例卡；战斗、社交、成长接受与旧 RPG 迁移均有自动回归 |

### R6. 长期记忆、失败、死亡与结局

**阶段结果**：长线世界能记住重要承诺和变化；失败继续产生故事，存档可主动结束并总结。

| ID | 小任务 | 依赖 | 验收 |
|---|---|---|---|
| R6.1 | 定义正式事件账本和来源 revision | R4 | 已在 WorldSave 顶层加入服务端维护的 `eventLedger`；opening / turn / growth / world-version-upgrade / migration 都记录稳定 ID、commandId 与 sourceRevision，并与短期 receipts 分离 |
| R6.2 | 从当前场景构建短期上下文窗口 | R6.1 | 已由 `buildWorldRecentContext()` 按存档 turns、待提交消息与账本位置变化即时重建；只投影到本次 Prompt history，不写入第二份事实 |
| R6.3 | 提取并校验长期事件记忆 | R6.1 | 已加入服务端规范化 `eventMemory`：AI 只能提交本回合候选，来源回合 / 事件 / revision 由提交服务绑定，并校验实体 / 地点 / 时间作用域 |
| R6.4 | 区分世界卡稳定设定与存档中已改变事实 | R6.1 | 已在 Prompt 明确 `WorldCard@worldVersion` 与 `WorldSave@revision` 两层来源；存档状态只解释当前局面，不回写卡片或被旧默认覆盖 |
| R6.5 | 按场景、NPC、目标和预算组装上下文 | R6.3 | 已按当前地点 / 目标 / NPC / 记忆裁剪；地图不注入全量区域，作用域段受 `prefs.worldContextBudget` 字符预算约束且关键状态优先 |
| R6.6 | 为派生记忆提供重建与调试视图 | R6.3 | 已提供存档级诊断与幂等重建；删除摘要后可从结构化事实重建，隐藏内容脱敏 |
| R6.7 | 定义失败与死亡模式 Schema | R5 | 已完成：世界卡声明模式，服务端在 HP 归零 / 冲突失败时结算，终止状态拒绝普通回合 |
| R6.8 | 定义开放式结局条件和玩家主动结束流程 | R6.7 | 不强制唯一结局；结束前明确确认 |
| R6.9 | 从正式事件生成世界线总结 | R6.8 | 总结覆盖人物经历、关系和世界变化，不虚构未提交事实 |
| R6.10 | 增加超长历史、失败继续、死亡和结局回归 | R6.9 | 重开后记忆、状态、结局和总结一致 |

### R7. 作者工作台与运行界面收口

**阶段结果**：卡作者能维护 R1–R6 已实际支持的规则；玩家能清楚查看世界变化，界面不再依赖固定 RPG 字段。

| ID | 小任务 | 依赖 | 验收 |
|---|---|---|---|
| R7.1 | 增加世界观与硬 / 软规则编辑区 | R1–R6 | 只编辑运行时已支持字段；硬规则错误可定位 |
| R7.2 | 增加玩家创建、属性、资源和特质编辑区 | R7.1 | 可添加、删除、排序并预览建角结果 |
| R7.3 | 增加 NPC、派系、地点和事件模板编辑区 | R7.1 | 跨实体引用使用稳定 ID 且可检查 |
| R7.4 | 增加判定、冲突、成长、失败和结局编辑区 | R7.1 | 不展示运行时尚未支持的空配置 |
| R7.5 | 为动态集合统一 ID 冲突和引用提示 | R7.2–R7.4 | 删除被引用实体前给出阻断或迁移报告 |
| R7.6 | 增加 JSON 原始视图与可视化表单双向校验 | R7.2–R7.4 | 非法 JSON 不覆盖最近有效草稿 |
| R7.7 | 增加发布前完整性检查 | R7.5 | 起点、规则、引用、Prompt 契约和 UI 投影错误阻止发布 |
| R7.8 | 把固定状态栏、背包和任务改为声明式投影 | R5.11 | 旧卡通过兼容投影继续显示 |
| R7.9 | 调试窗口增加动作解析、判定、候选差异和提交 receipt | R2 | 默认脱敏 API key、隐藏秘密和本机路径 |
| R7.10 | 完成桌面 / 窄屏 / 键盘 / 焦点 / reduced-motion 检查 | R7.8 | 真实浏览器关键流程可操作 |
| R7.11 | 只在自然边界拆分 `app.js`，不做大爆炸重写 | R7.1 | 每次拆分都有现有行为回归；无双重状态 owner |
| R7.12 | 建立两张官方示例卡：福瑞西幻与非魔法题材 | R7.8 | 证明卡驱动字段、资源、冲突和 UI，而非前端写死 |

### R8. 存档操作、平台与发布验收

**阶段结果**：Web 主链可发布，PWA / Android 语义对齐，用户数据可备份和恢复。

| ID | 小任务 | 依赖 | 验收 |
|---|---|---|---|
| R8.1 | 增加存档重命名和导出 | R6 | 导出不含 API key、本机路径或其他存档数据 |
| R8.2 | 增加存档复制并重新命名所有 owner / receipt | R8.1 | 复制后两条世界线完全独立 |
| R8.3 | 增加显式删除与可恢复保护 | R8.1 | 精确显示目标世界 / 存档；取消不写入 |
| R8.4 | 定义 v1 / v2 卡和存档迁移、dry-run、备份和回退 | R7 | 失败不覆盖原件，重复执行幂等 |
| R8.5 | 为完整玩家闭环补浏览器自动化 | R7.7 | 建角、开场、行动、判定、NPC、时间、重开全部通过 |
| R8.6 | 做双世界 / 双存档 / Tavern 交叉隔离回归 | R8.5 | Prompt、状态、地图、图片、记忆和 UI 无串联 |
| R8.7 | 对齐 Android 世界 / 存档 / 回合 / 导入 API | R8.4 | Android 不再回退到旧 `session.rpgState` 主链 |
| R8.8 | 构建并实机验证 PWA 与 Android | R8.7 | 断网壳、重启恢复、图片缺失和超时恢复可用 |
| R8.9 | 完成安全复核 | R8.4 | 导入内容不执行脚本；路径、秘密、Prompt 注入和超大数据受控 |
| R8.10 | 同步 README、数据结构、卡作者指南和迁移指南 | R8.9 | 文档只描述已实现行为和已验证平台 |

## 4. 实施顺序与阶段门

```text
B0 已有底座
  ↓
R0 产品契约
  ↓
R1 建角并开局
  ↓
R2 可信的一回合
  ↓
R3 NPC 持续性
  ↓
R4 时间 / 事件 / 开放目标
  ↓
R5 冲突 / 资源 / 成长
  ↓
R6 记忆 / 失败 / 结局
  ↓
R7 作者工作台与动态 UI
  ↓
R8 平台与发布验收
```

每个阶段只有同时满足以下条件才进入下一阶段：

1. 玩家可以从入口完成该阶段的可见结果，不依赖手改 JSON。
2. 所有新动态数据有明确 `saveId` owner，切换世界和存档不串数据。
3. 网络、模型、解析、校验、保存失败都有明确恢复路径，且不产生半提交状态。
4. Node 最小回归通过；涉及页面时必须补真实浏览器验证。
5. 开工前完成针对该切片的 GitHub / 官方资料检索，记录许可证和“不重复造轮子”结论。
6. 只做本地 commit，不 push；APK 和 Actions 仅在用户明确要求时执行。

## 5. 当前建议的第一批任务

不要继续直接做 R7 的“大而全编辑器”。当前最短的产品路径是：

1. **R0.2–R0.7**：冻结首版玩家控制、建议行动、WorldCard v2 和回合契约。
2. **R1.1–R1.7**：先让玩家能依据卡规则创建角色，并安全写入独立存档。
3. **R1.8–R1.10**：生成、提交和重开一个真正属于该角色与世界的开场。
4. 然后进入 **R2**，替换当前“叙事中识别骰子 + 固定 HP / MP JSON”的临时玩法内核。

第一批完成后，项目才会从“世界存档技术演示”跨到“可开始游玩的 RPG”。

## 6. 当前验证证据与限制

2026-08-13 已通过：

```text
node --check server.js
node --check public/app.js
node --check public/mapgen.js
node scripts/check_character_fields.js
node scripts/check_prompt_presets.js
node scripts/check_session_binding.js
node scripts/check_world_storage.js
node scripts/check_recovery.js
node scripts/check_pwa.js
```

### R4.11 Current progress

Implemented the minimal faction contract: static `WorldCard.factions`, save-owned `WorldSave.state.factionStates`, server-side allowlist/range validation, initialization on create and world upgrade, and RPG panel/prompt projection. R4.12 adds time-point faction actions and event projection.

### R4.12 Current progress

已完成最小派系行动闭环：世界卡可声明带时间 / 地点条件的一次性行动，正式回合推进后服务端原子更新当前存档的关系、影响力和资源，并投影为带来源的世界事件；稳定事件 ID 与 receipt 防止重复提交重复行动，Prompt 仅注入当前地点相关或最近发生的派系。

当前限制：

- 本轮没有浏览器控制授权，因此不能把真实页面闭环标为已验收。
- `android/.../TavernServer.kt` 仍只实现聊天、图片、模型与旧数据 API，没有 Web 端世界卡 / 世界存档主链。
- `public/app.js` 仍是单一大文件；后续只在功能自然边界出现时小步拆分，不先做无玩法收益的重构。
- 现有 `_defaults.json` 只有一个极小世界种子和固定 RPG 状态，不能作为完整玩法完成度的证据。

### R5.4 当前进度

已完成最小可用切片：`playerCreation.derived` 支持安全四则表达式、跨派生值引用、循环与非法引用校验；派生值只读计算，不进入可写存档状态。后续再扩展条件、取整或百分比语法时必须保持无 `eval`、有上限、服务端复核。

### R5.5 当前进度

已完成最小经济闭环：世界卡可声明物品目录、装备位、货币范围、背包格数与重量上限；创建存档、普通保存和正式回合提交都会校验物品 ID、堆叠、装备位引用、货币范围与负重。前端同步显示背包容量、装备位和货币；未声明 `economy` 的旧世界继续走兼容路径。

### R5.6 当前进度

已完成统一冲突状态最小闭环：世界卡声明冲突类型、阶段、行动与结果；当前存档持有独立 `state.conflicts` 实例；AI 通过 `start / advance / end` 候选推进，服务端校验模板引用、轮次、阶段、行动、结束结果并在 receipt 记录生命周期变化。前端显示当前冲突和阶段；战斗数值由 R5.7 补齐，非战斗资源差异留给 R5.8。

### R5.7 当前进度

已完成最小战斗闭环：战斗行动可声明 `check`（攻击骰、属性 / 技能修正、目标值和伤害骰），AI 只提交行动与 `targetId`；正式回合由服务端用当前存档玩家数值掷攻击骰，命中后才掷伤害骰并写回目标参与者 HP。参与者的 `hp/maxHp/defense` 由服务端保护，结果写入当前回合 receipt 的 `combatChecks`，撤退与失败仍沿用冲突 outcome。

### R5.8 当前进度

已完成最小非战斗闭环：默认世界卡提供谈判与潜行模板；social / stealth 行动可声明 `d20 + 技能修正 vs target`，服务端只把判定写入 `conflictChecks`，不读取或改变参与者 HP。最近判定会进入 RPG Prompt，AI 可据此决定继续推进或以 declared outcome 结束冲突。

### R5.9 当前进度

已完成成长候选最小闭环：`playerCreation.growth` 声明来源与允许的属性 / 技能 / 资源候选；当前存档使用 `state.growthCandidates` 保存 `proposed` 候选及理由。AI 只能提出候选，服务端按世界卡白名单复核，暂不自动改变玩家数值，避免把叙事奖励误写成正式成长。

### R5.10 当前进度

已完成成长应用闭环：玩家可在当前存档面板接受或拒绝候选；服务端按世界卡声明应用属性、技能、资源、特质、NPC 关系、阵营声望或身份标签。处理记录写入 `state.growthApplications`，接受结果另外写入 `state.experiences`，包含来源、效果、地点和 revision；AI 不能直接把候选变成已应用成长。

### R5.11 当前进度

已完成动态 RPG 面板：属性、技能、资源、派生值、特质、关系、身份和状态效果由 `playerCreation` 与 `state.player` 投影；装备位和物品名称读取经济规则，冲突读取声明模板与存档实例。旧存档缺少动态玩家状态时仍显示原有兼容栏位，不阻塞进入工作区。

### R5.12 当前进度

已加入 `world-grey-harbor`（现代悬疑 / 非魔法）与 `world-orbit-station`（科幻 / 轨道站）两张示例世界卡。`scripts/check_rpg_regression.js` 会逐张创建独立存档，启动并推进 combat + social 冲突，接受第一项成长并重新读取存档；同时验证旧 RPG 会话可迁移到示例世界，成长数组保持空且不串存档。

### R6.1 当前进度

已加入 WorldSave 顶层 `eventLedger`（当前最多 4096 条）。开场、正式回合、成长处理、世界版本升级和旧 RPG 迁移都会在服务端追加稳定账本记录；每条记录带 `commandId`、`sourceRevision`、时间 / 地点作用域，并引用已提交的回合、世界事件或成长应用。账本独立于最多 200 条的 `receipts`，为后续短期窗口和长期记忆提供可回溯来源。

### R6.2 当前进度

已完成可重建的短期上下文窗口：RPG Prompt 发送前从当前存档的 `turns`、待提交消息和 `eventLedger` 推导最近 N 条 history；检测到账本记录的位置切换时，优先保留新地点场景消息。窗口只存在于请求构建结果，不写回 `WorldSave`，因此刷新或删除派生结果不会改变正式事实。

### R6.3 当前进度

已完成长期事件记忆的最小闭环：RPG 控制块可提交 `eventMemory` 候选，服务端只在正式回合成功提交时写入规范化条目，并自动绑定本回合 turn ID、结算事件 ID、source revision、地点与时间。存档最多保留 512 条，Prompt 仅注入当前地点可见的记忆；AI 无法直接写入来源字段或改写旧记忆。

### R6.4 当前进度

已完成事实分层投影：Prompt 明确标注稳定世界卡版本与当前世界存档 revision，并规定同一实体 / 地点同时存在静态资料和运行时状态时，静态资料只代表默认设定，存档状态解释当前局面。实现没有复制第二份“当前世界”或反向改写已发布世界卡。

### R6.5 当前进度

已完成请求级上下文裁剪：NPC 会带入当前地点、队伍、任务 / 目标 / 线索、冲突参与者与长期记忆命中的实体；事件只保留当前地点的本地记录和最近公共记录；地图只保留当前位置及相邻区域。所有世界作用域 Prompt 段按状态、目标、记忆、NPC、事件、地图 / 派系排序，受 `prefs.worldContextBudget`（默认 24000 字符）限制，裁剪不会写回存档。

### R6.6 当前进度

已完成派生记忆的诊断与重建闭环：独立调试终端新增 MEMORY 视图，显示当前存档 revision、记忆可见性统计、正式来源计数和脱敏重建预览；服务端通过 `/memory` 与 `/memory/rebuild` 提供只读诊断和幂等重建。重建只读取 `state.worldEvents`、成长应用、经历与 `eventLedger` 来源引用，不改变正式世界 revision；隐藏事件不在诊断预览中展开。

### R6.7 当前进度

已完成失败与死亡模式最小闭环：`WorldCard.failure` 声明默认模式、HP 归零 / 冲突失败选择器和自定义模式；`WorldSave.state.failure` 只由服务端在回合提交时结算，支持继续、重伤、俘虏、资源损失、永久死亡与卡定义。终止状态拒绝新的普通回合，客户端 / AI 不能伪造或清除失败状态；世界卡草稿、导入校验、RPG Prompt 与状态面板已同步。

### R4.10 当前进度

已完成截止时限的最小结算：目标 / 线索使用绝对世界时钟，服务端在正式回合推进后自动标记过期，receipt 记录过期 ID，前端和 RPG Prompt 显示未过期时限。长休、移动成本和更丰富的跨地区后果留给后续 `timeCost` 切片。
